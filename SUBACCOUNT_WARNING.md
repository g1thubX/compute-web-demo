# ⚠️ 推理子账户资金重要提示

## 关键信息

**推理子账户的资金无法直接提现**

这是 0G Broker 系统的设计特性，用户需要理解其含义。

---

## 快速理解

### 两个账户的区别

```
主账户 (Main Account)          推理子账户 (Inference Sub-Account)
├─ 可以充值 ✅                 ├─ 不能充值 ❌
├─ 可以提现 ✅                 ├─ 不能提现 ❌
├─ 管理总资金 ✅               ├─ 用于 AI 调用 ✅
├─ 支持转账到子账户 ✅         ├─ 自动扣费 ✅
└─ 随时提现钱包 ✅             └─ 无法转回 ❌
```

### 资金流向

```
您的钱包
   ↓ 充值
主账户 (可提现)
   ↓ 转账
推理子账户 (不可提现)
   ↓ 消耗
AI 调用费用
```

---

## 最重要的三点

### 1️⃣ 谨慎转账

**转账到子账户前，请确认：**
- ✅ 真的需要这么多资金
- ✅ 已经计算过 AI 调用成本
- ✅ 预留了适当的缓冲 (20-30%)

```typescript
// ❌ 错误做法
await transferFund(address, "inference", 10e18); // 一次性转 10
// 现在 9 个token 被困在子账户

// ✅ 正确做法
const estimatedCost = queryCount * 0.5 * 1.2; // 带缓冲
await transferFund(address, "inference", estimatedCost);
```

### 2️⃣ 主账户是生命线

**始终保留足够的主账户余额，因为：**
- 主账户才能提现
- 主账户才能充值
- 主账户是转账的来源

```typescript
// 充值 10 token 时的推荐分配
总充值: 10 token
├─ 主账户保留: 8 token (随时可提现)
└─ 转账到子账户: 2 token (用于 AI)
```

### 3️⃣ 估算成本很重要

**在转账前计算所需资金：**

```typescript
const expectedQueries = 100;  // 预计 100 次查询
const costPerQuery = 0.5;     // 每次 0.5 token
const buffer = 1.2;           // 20% 缓冲
const required = expectedQueries * costPerQuery * buffer;

console.log(`需要转账: ${required} token`); // 需要转账: 60 token
```

---

## 设计背后的原因

### 为什么不能从子账户直接提现？

| 理由 | 说明 |
|------|------|
| **安全性** | 防止意外资金流出，子账户只用于推理 |
| **成本追踪** | 便于精确统计 AI 调用成本 |
| **资金隔离** | 不同功能的资金分开管理 |
| **防止滥用** | 确保资金用于既定目的 |

---

## 常见错误 & 解决方案

### ❌ 错误 1: 一次性转账过多

```typescript
// 转了 20 token，只用了 2 token
await transferFund(address, "inference", 20e18);

// 结果：18 token 被困在子账户
```

**解决**:
- ✅ 下次分次转账：每次转 1-2 token
- ✅ 计算好需求后再转
- ✅ 预留主账户余额用于提现

### ❌ 错误 2: 转账后才想提现

```typescript
// 已经转了所有资金到子账户
await transferFund(address, "inference", 10e18);

// 现在想提现，但主账户是空的
await withdrawFund(5); // ❌ 没有足够余额
```

**解决**:
- ✅ 事先保留足够的主账户余额
- ✅ 分批转账，不是一次性全转

### ❌ 错误 3: 频繁小额转账

```typescript
// 每次都只转 0.1 token
for (let i = 0; i < 100; i++) {
  await transferFund(address, "inference", 0.1e18); // ❌ 100 次交易
}
```

**解决**:
- ✅ 一次转足够的金额
- ✅ 估算好总需求，批量转账

---

## 推荐操作流程

### 第一次使用

```
1. 充值到主账户
   └─ 充值 10 token (作为例子)

2. 估算需求
   └─ 计算: 100 次查询 × 0.5 token + 20% 缓冲 = 60 token

3. 保留主账户余额
   └─ 主账户保留: 10 - 5 = 5 token (可以提现)

4. 转账到子账户
   └─ 只转 5 token 用于 AI

5. 使用 AI
   └─ 调用 AI API，自动扣费

6. 提现主账户
   └─ 提现剩余的 5 token
```

### 正式使用

```
周期性充值策略:

1. 监控子账户余额
   └─ setInterval(() => { checkBalance() }, 3600000); // 每小时检查

2. 当余额低于阈值时充值
   └─ if (subBalance < 1) { refill(); }

3. 每月提现一次
   └─ await withdrawFund(mainBalance * 0.9); // 提现 90%
```

---

## 重要警告

### 🚨 请理解这些限制

- 子账户资金**不能转回**主账户
- 子账户资金**不能直接提现**到钱包
- 子账户资金**无法退款**
- 一旦转账，资金就被**"锁定"**在子账户

### 🔐 保护自己的方法

1. **小额试验**
   ```
   第一次: 充值 1-2 token 测试
   了解成本后再大额充值
   ```

2. **分批转账**
   ```
   不要一次性转所有资金
   根据实际使用分次转账
   ```

3. **保留主账户余额**
   ```
   充值的 30-50% 保留在主账户
   用于随时提现或应急
   ```

4. **定期监控**
   ```
   定期检查子账户余额
   避免意外的资金耗尽
   ```

---

## 数据示例

### 场景：我有 100 USDT

**❌ 不推荐的做法**:
```
充值: 100 USDT 到主账户
转账: 100 USDT 到子账户
结果: 主账户为 0，全部被困
```

**✅ 推荐的做法**:
```
充值: 100 USDT 到主账户
保留: 50 USDT 在主账户（可提现）
转账: 50 USDT 到子账户（用于 AI）

这样：
- 可以随时提现 50 USDT
- 可以进行 ~100 次 AI 查询（50 / 0.5）
- 保持资金流动性
```

---

## 联系与反馈

如果您认为这个设计不合理，可以：

1. **提 GitHub Issue**
   - 仓库: https://github.com/0glabs/0g-serving-broker

2. **加入社区讨论**
   - Discord: 0G Labs 社区
   - 论坛: 0G 官方论坛

3. **发送反馈**
   - Email: feedback@0g.ai

---

## 总结

| 操作 | 结果 | 风险 |
|------|------|------|
| 充值到主账户 | 资金可用 | 无 |
| 转账到子账户 | 进行 AI 调用 | **无法回收** ⚠️ |
| 从主账户提现 | 回到钱包 | 无 |
| ~~从子账户提现~~ | ❌ 不支持 | 不可用 |

---

**最后提醒**: 
> 转账到子账户前，请确保您真的需要这些资金用于 AI 调用。这不是一个可逆的操作！

